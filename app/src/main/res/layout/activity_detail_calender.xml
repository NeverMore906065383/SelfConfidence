<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.activity.DetailCalenderActivity">

    <data>

        <variable
            name="data"
            type="com.example.selfconfidence.viewmodel.activity.DetailCalenderViewModel" />

    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        app:layout_anchorGravity="center">

        <TextView
            android:id="@+id/tv_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{data.model.message}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <CheckBox
            android:id="@+id/checkbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="@{data.model.status==0?true:false}"
            android:text="转态"
            app:layout_constraintLeft_toRightOf="@+id/tv_message"
            app:layout_constraintTop_toTopOf="@+id/tv_message" />

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>
